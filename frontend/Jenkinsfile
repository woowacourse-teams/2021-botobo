pipeline {
     agent any
     tools {
        nodejs "nodejs"
        git "git"
    }
     stages {
        stage("Build") {
            steps {
                sh "yarn"
                sh "yarn build:dev"
            }
        }
    }
}
